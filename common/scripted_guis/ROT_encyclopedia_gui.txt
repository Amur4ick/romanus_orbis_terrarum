scripted_gui = {

	# КЉнопка в топбаре
	
	encyclopedia_button_gui = {
	
		context_type = player_context
		window_name = "ROT_encyclopedia_button_window"
		parent_window_token = top_bar 
		
		visible = {
			is_ai = no
		}
		
		effects = {
			encyclopedia_button_click = {
				if = {
					limit = {
						check_variable = { show_encyclopedia_window = 1 }
					}
					set_variable = { show_encyclopedia_window = 0 }
				}
				else = {
					set_variable = { show_encyclopedia_window = 1 }
				}
			}
		}
		
		triggers = {
			
		}
		
	}
	
	encyclopedia_gui = {
	
		context_type = player_context
		window_name = "ROT_encyclopedia_main_window"
		#parent_window_token = top_bar 
		
		visible = {
			is_ai = no
			check_variable = { show_encyclopedia_window = 1 }
		}
		
		effects = {
		
		}
		
		triggers = {
			countries_button_click_enabled = {
				always = no
			}
			ideologies_button_click_enabled = {
				always = no
			}
		}
		
	}
}