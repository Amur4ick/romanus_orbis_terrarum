scripted_gui = {
	
	laws_button_window_gui = {
	
		context_type = player_context
		window_name = "ROT_laws_button_window"
		parent_window_token = politics_tab 
		
		visible = {
			always = yes
		}
		
		effects = {
			parlament_button_click = {
				if = {
					limit = {
						check_variable = { show_laws_window = 1 }
					}
					set_variable = { show_laws_window = 0 }
				}
				else = {
					set_variable = { show_laws_window = 1 }
				}
			}
		}
		
		triggers = {
			
		}
		
	}
	
}